# den — 프로젝트 관리 + 메신저 사내 솔루션

## 브랜드 표기 (중요)
제품명은 **항상 소문자 `den`**. 문장 첫머리에서도 대문자화하지 않는다.
UI 로고, 사이드바, 타이틀, 패키지명 모두 소문자로 표기할 것.
- 올바름: `den`
- 금지: `Den`, `DEN`

조직명은 **pleanb**(소문자). 컨셉: "탈출해서 린하게 계획하는 우리들의 아지트".

## 지금 단계의 목표 (매우 중요)

**백엔드 없이, 목업 데이터만으로 전체 화면과 흐름이 돌아가는 것처럼 보이는 프론트엔드를 완성한다.**

- 실제 API 호출 없음. `src/mock/` 의 목업 데이터로 동작
- 화면 간 이동, 필터, 상세 열기, 상태 변경 등이 **실제로 동작하는 것처럼** 보여야 함
- 목업이라도 데이터는 메모리에서 실제로 변경되어야 함 (상태 변경 시 리스트에 반영 등)
- 백엔드는 이 프론트가 완성된 후 별도로 구현. 지금은 신경 쓰지 말 것

---

## 기술 스택 (확정 — 변경 금지)

- **Vue 3 + TypeScript + Vite** (React/Nuxt 아님. 이유는 docs/DEN-DESIGN.md 9장)
- **Vue Router** — 라우팅
- **Pinia** — 전역 상태
- **@tanstack/vue-query** — 서버 상태·캐싱 (목업 단계에서도 사용, 나중에 API로 교체)
- **shadcn-vue + Tailwind CSS v4** — UI
- **frappe-gantt** (MIT) — 간트차트
- **@stomp/stompjs** — 메신저 (나중에)
- **axios** — HTTP (나중에)

---

## 반드시 지킬 규칙

### 1. URL 우선 원칙 (가장 중요)
"뒤로가기로 복원되어야 하는 상태"는 **반드시 URL 쿼리스트링에** 둔다.

- 대상: 페이지네이션, 필터(상태/담당자/우선순위/태그), 뷰 전환(리스트/간트), 그룹핑, 검색어
- 구현: Vue Router의 `route.query` 사용
- **금지**: 위 항목들을 `ref`/`reactive`에만 두는 것

```ts
// 올바름
const page = computed(() => Number(route.query.page ?? 1))
router.push({ query: { ...route.query, page: 8 } })

// 금지 — 뒤로가기 시 1페이지로 리셋됨
const page = ref(1)
```

이유: 이 규칙을 어기면 목록 8페이지 → 상세 → 뒤로가기 시 1페이지로 리셋되는
문제가 발생한다. HTML 게시판처럼 동작해야 한다.

### 2. 상태 배치 규칙
| 종류 | 위치 |
|---|---|
| 서버(목업) 데이터 | @tanstack/vue-query |
| 뒤로가기 복원 대상 (필터·뷰·페이지) | URL (`route.query`) |
| 진짜 전역 (로그인 사용자, 알림 카운트) | Pinia |
| 화면 일시 상태 (드롭다운 열림 등) | `ref` / `reactive` |

### 3. 디자인 토큰만 사용 (하드코딩 금지)
- 색상·모서리·간격은 **반드시 `src/index.css`의 CSS 변수**를 통해 사용
- **금지**: `color: #4f46e5` 같은 하드코딩, 임의의 Tailwind 색상 클래스(`bg-blue-500` 등)
- **사용**: `bg-primary`, `text-muted-foreground`, `border-border`, `rounded-lg` 등 토큰 기반 클래스
- 업무 상태색/우선순위색은 `src/shared/lib/constants.ts` 의 매핑을 통해서만 사용

### 4. 디자인 레퍼런스
`design-reference/` 폴더에 확정된 디자인의 HTML 샘플이 있다.
**새 화면을 만들 때 반드시 먼저 열어보고 그 톤·구조·간격을 따를 것.**
- `design-reference/project-list-v2.html` — 업무 리스트 화면 + 멀티프로젝트 사이드바
- `design-reference/all-projects-home.html` — 전체 프로젝트 홈 (카드 그리드)

### 5. 폴더 구조 (feature 기반)
```
src/
├── router/           라우팅 정의
├── layouts/          AppLayout, Sidebar, ContextBar
├── components/ui/    shadcn-vue 컴포넌트
├── shared/
│   ├── lib/          utils, constants (상태·우선순위 매핑)
│   └── store/        Pinia 스토어
├── mock/             목업 데이터 + 목업 API 함수
└── features/
    ├── projects/     전체 프로젝트 홈
    ├── tasks/        리스트·간트·상세·댓글
    ├── messenger/
    ├── permissions/  멤버·역할 설정
    └── notifications/
```

---

## 디자인 시스템

**컨셉: Notion의 밝기 + Linear의 구조 + 인디고 액센트 + 큰 둥근 모서리 + 라이트 모드**

| 요소 | 값 |
|---|---|
| 배경 | `#f7f8fa` 밝은 회백색 (순백 아님) |
| 카드 | `#ffffff` + 미세 그림자 + 큰 둥근 모서리 |
| 모서리 반경 | 14px |
| 텍스트 | `#1e2230` 짙은 회색 (순검정 아님) |
| 보조 텍스트 | `#6b7280` |
| 경계선 | `#eceef2` (얇게) |
| **액센트** | **인디고 `#4f46e5`** — 버튼·활성·링크·선택·포커스 |

**업무 상태색** (데이터 전용):
- 할 일 `#f1f2f5` / `#565d6d`
- 진행 중 `#eef2ff` / `#4f46e5`
- 검토 `#fff4e5` / `#b76a09`
- 완료 `#e7f6ee` / `#1a9457`

**우선순위**: 긴급 `#e5484d` / 높음 `#f2820c` / 보통 `#4f46e5` / 낮음 `#9ca3af`

원칙: **UI 강조는 인디고 하나, 색은 데이터/상태에만.** 나머지는 무채색.
다크 모드는 지금 만들지 않는다 (라이트 전용).

---

## 화면 구조

### 라우팅
```
/login                                로그인
/                                     전체 프로젝트 홈
/p/:projectKey/tasks                  업무 (리스트/간트)
    ?view=list|gantt
    ?status=&assignee=&priority=&tag=
    ?group=status|assignee|none
    ?page=
/p/:projectKey/tasks/:taskId          업무 상세 (패널, URL 변경됨)
/p/:projectKey/messenger[/:channelId] 메신저
/p/:projectKey/settings/members       멤버 관리
/p/:projectKey/settings/roles         역할·권한
/notifications                        알림 전체
```
- `projectKey`는 사람이 읽는 키("APP"). uuid 노출 금지.

### 레이아웃 (멀티 프로젝트 대응 — 50~100개 프로젝트 전제)
**사이드바** (프로젝트 탐색 전용):
- 상단 프로젝트 검색
- 고정 메뉴: 전체 프로젝트 / 내 업무 / 최근 본 항목
- 즐겨찾기 섹션
- 폴더 그룹핑 (접기/펼치기, 개인용 정리함)

**상단 컨텍스트 바** (선택된 프로젝트 안의 메뉴):
- 프로젝트명 + 탭: 업무 / 간트차트 / 메신저 / 설정
- 메뉴 권한에 따라 탭 표시/숨김
- 업무 검색, 알림 벨

> 업무/간트/메신저 탭을 사이드바가 아닌 상단에 두는 이유: 이들은 "특정 프로젝트
> 안의 메뉴"라 사이드바(프로젝트 목록)와 충돌하기 때문. 이 분리를 지킬 것.

---

## 작업 순서 (이 순서대로 진행)

1. **공통 토대** — 스캐폴드, 레이아웃(사이드바+컨텍스트바), 라우팅, 목업 데이터 셋업
2. **전체 프로젝트 홈** — 카드 그리드, 즐겨찾기/폴더 섹션, 검색
3. **업무 리스트 뷰** — 리스트, 상태/우선순위 배지, 필터 칩, 그룹핑, 페이지네이션
4. **업무 상세 패널** — 담당자(다대다), 상태·우선순위·기간·진행률 편집, 댓글·멘션, 의존성, 하위업무
5. **권한·멤버 설정** — 멤버 초대/역할 지정, 역할별 메뉴 권한 토글
6. **간트 뷰** — frappe-gantt, 의존성 표시, 디자인 토큰으로 스타일 통일
7. **메신저** — 채널 목록, 채팅 UI (실시간은 목업으로 시뮬레이션)
8. **알림** — 알림 목록, 벨 드롭다운, 안읽음 배지

**한 번에 하나씩.** 각 단계에서 그 화면에 딸린 모달·팝오버·인라인 편집 조각까지
완성한 후 다음으로 넘어간다. 완료되면 사용자에게 확인받고 진행할 것.

---

## 도메인 규칙 (목업 데이터에도 반영)

### 권한 3단
1. **프로젝트 권한** — 역할 기반 (관리자/편집자/뷰어). 멤버 아니면 프로젝트 안 보임
2. **메뉴 권한** — 역할별로 메뉴(업무/간트/메신저) 접근 on/off. 보기/편집 구분 없음
3. **업무 권한** — 업무별 `isPrivate` 플래그. true면 담당자+참여자만 조회 가능

### 업무 데이터 형태
- 담당자: **다대다** (여러 명 가능)
- 계층: `parentId`로 부모-자식
- 의존성: 선행 업무 목록 (간트용)
- 기간: `startDate`, `endDate`, `progress`(0-100)
- 상태: todo / progress / review / done
- 우선순위: urgent / high / medium / low
- 태그: 프로젝트별 정의, 업무에 다대다

### 폴더 (프로젝트 정리)
- **개인용 정리함**. 권한과 무관
- 같은 프로젝트도 사람마다 다른 폴더에 배치 가능
- 폴더에 안 넣은 프로젝트는 "미분류"로 최상위 표시
- 폴더 삭제해도 프로젝트는 사라지지 않고 미분류로 이동

---

## 하지 말 것

- ❌ React 문법·라이브러리 사용 (이 프로젝트는 Vue)
- ❌ Nuxt 도입 (순수 Vue + Vite)
- ❌ 색상·간격 하드코딩 (토큰 사용)
- ❌ 필터·페이지 상태를 `ref`에만 두기 (URL에 둘 것)
- ❌ 백엔드 API 호출 시도 (지금은 목업만)
- ❌ 다크 모드 구현 (라이트 전용)
- ❌ 칸반 보드 (지금 범위 밖)
- ❌ 한 번에 여러 단계 진행 (한 단계씩, 확인받고 진행)

---

## 참고 문서

- `docs/DEN-DESIGN.md` — 전체 설계 상세 (데이터 모델, API 스펙, 결정 로그)
- `design-reference/*.html` — 확정된 디자인 샘플 (새 화면 만들기 전 반드시 확인)

응답은 한국어로 한다.
